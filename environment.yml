name: apr_prof_env
channels:
  - conda-forge
dependencies:
  - python=3.12
  - numpy
  - matplotlib
  - jupyter
  - ipykernel
  # agregados para la Pregunta 2
  - pandas
  - scikit-learn
  - pytorch>=2.4        # desde conda-forge: el del canal pytorch se compila contra numpy 1.x
  - gdown               # descarga del dataset desde el link de Drive
